# Portfólio

As imagens `work-01.jpg` a `work-12.jpg` foram obtidas do perfil público oficial `@gbtattoomcz_oficial` em 18/09/2026. Para atualizar o portfólio, substitua os arquivos mantendo os mesmos nomes ou ajuste a lista `portfolioItems` em `app/page.tsx`.

`artist.jpg` é a foto pública do perfil e pode ser substituída por um retrato profissional em maior resolução.
